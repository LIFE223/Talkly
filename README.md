# Talky – GitHub-backed, Encrypted Chats + Call Signaling

All storage is via GitHub JSON; messages are encrypted client-side using a chat key
derived with SHA-256 from a human-readable code. See app.js comments for details.
